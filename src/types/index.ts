export type ProjectType = {
  title: string;
  description: string;
  liveLink: string;
  codeLink: string;
  link: string;
  image: string;
  overview: string;
  mainDescription: string;
  tools: string[];
};

export type SocialTypes = { link: string; icon: string };
